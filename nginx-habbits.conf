# Nginx конфигурация для habbits
# Скопировать в /etc/nginx/sites-available/habbits
# Создать симлинк: ln -s /etc/nginx/sites-available/habbits /etc/nginx/sites-enabled/

server {
    listen 80;
    server_name habbits.your-domain.com;  # Замените на ваш домен или IP
    
    # Редирект на HTTPS (раскомментировать после настройки SSL)
    # return 301 https://$server_name$request_uri;
    
    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# HTTPS конфигурация (активируется после certbot)
# server {
#     listen 443 ssl http2;
#     server_name habbits.your-domain.com;
#
#     ssl_certificate /etc/letsencrypt/live/habbits.your-domain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/habbits.your-domain.com/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
# }
